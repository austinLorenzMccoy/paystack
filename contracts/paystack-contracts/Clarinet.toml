[project]
name = "x402pay-contracts"
description = ""
authors = []
telemetry = true
cache_dir = "./.cache"

[contracts.revenue-split]
path = "contracts/revenue-split.clar"
clarity_version = 2
epoch = 2.4

[contracts.subscription-manager]
path = "contracts/subscription-manager.clar"
clarity_version = 2
epoch = 2.4

[contracts.escrow-refund]
path = "contracts/escrow-refund.clar"
clarity_version = 2
epoch = 2.4

[contracts.time-gated-access]
path = "contracts/time-gated-access.clar"
clarity_version = 2
epoch = 2.4

[contracts.royalty-cascade]
path = "contracts/royalty-cascade.clar"
clarity_version = 2
epoch = 2.4

[contracts.tiered-pricing]
path = "contracts/tiered-pricing.clar"
clarity_version = 2
epoch = 2.4

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }

# Check-checker settings:
# trusted_sender: if true, inputs are trusted after tx_sender has been checked.
# trusted_caller: if true, inputs are trusted after contract-caller has been checked.
# callee_filter: if true, untrusted data may be passed into a private function without a
# warning, if it gets checked inside. This check will also propagate up to the
# caller.
# More informations: https://www.hiro.so/blog/new-safety-checks-in-clarinet
